name: Build and Push to GHCR

on:
  workflow_dispatch:  # manually trigger from GitHub UI
  push:
    branches: main

env:
  REGISTRY: ghcr.io
  {% raw %}
  IMAGE_NAME: ${{ github.repository }}
  {% endraw %}

jobs:
  docker:
    runs-on: ubuntu-latest
    env:
      {% raw %}
      KUBECONFIG: ${{ github.workspace }}/kubeconfig.yaml
      {% endraw %}
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          {% raw %}
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          {% endraw %}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          {% raw %}
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          {% endraw %}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          {% raw %}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          {% endraw %}

      # - name: Deploy to OpenChoreo
      #   run: |
      #     curl -X POST "https://3a8600b737ff.ngrok.app/deployments" \
      #       -H "Content-Type: application/json" \
      #       --data @.github/deploy_component_request.json

      # - name: Set up kubeconfig
      #   run: |
      #     echo "\${{ secrets.KUBECONFIG }}" > kubeconfig.yaml

      # - name: Verify k8s cluster connection
      #   run: kubectl get nodes

      # - name: Apply OpenChoreo manifests
      #   run: kubectl apply -f openchoreo.yaml